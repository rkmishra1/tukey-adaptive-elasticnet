% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{plot.tukeyAdEnetRBIC}
\alias{plot.tukeyAdEnetRBIC}
\title{Plot the robust BIC tuning path}
\usage{
\method{plot}{tukeyAdEnetRBIC}(x, y, ...)
}
\arguments{
\item{x}{A `"tukeyAdEnetRBIC"` object as returned by [tukeyAdEnetRBIC()].}

\item{y}{Unused; included for S3 method consistency.}

\item{...}{Additional arguments passed to [graphics::plot()].}
}
\value{
The RBIC grid (invisibly), as a data frame.
}
\description{
Plots the robust BIC criterion against `lambda1`, one line per distinct
value of `lambda2`, with the selected `(lambda1, lambda2)` pair marked.
}
\examples{
set.seed(1)
n <- 100; p <- 10
x <- matrix(rnorm(n * p), n, p)
beta <- c(2, -2, 1.5, rep(0, p - 3))
y <- as.numeric(x \%*\% beta + rnorm(n))
\donttest{
tuned <- tukeyAdEnetRBIC(x, y, n_lambda1 = 8, lambda2_factors = c(0, 0.1))
plot(tuned)
}

}
